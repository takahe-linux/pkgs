# Maintainer: Alastair Hughes < hobbitalastair at yandex dot com>

# Package information
pkgname='packages'
pkgver='1.0'
pkgrel=69
pkgdesc="Meta-package for the cross-compiled packages"

# Additional information...
arch=('any')

# Dependency information.
# We need base-devel and enough to boot.
depends=(
    # base-devel:
    'busybox'
    'binutils'
    'bison'
    'coreutils'
    'fakeroot'
    'file'
    'filesystem'
    'findutils'
    'flex'
    'gcc'
    'gettext'
    'linux-api-headers'
    'm4'
    'make'
    'musl'
    'ncurses'
    'openssl'
    'pacman'
    'pkg-config'
    'util-linux'
    'xz'

    # Required for booting:
    'initscripts'
    'mdev'
    'sinit'
    'linux'
    )

package() {
    :
}

# Maintainer: Alastair Hughes < hobbitalastair at yandex dot com>

# Package information
pkgname='musl'
pkgver='1.1.12'
pkgrel=1
pkgdesc="Lightweight libc implementation"

# Additional information...
arch=('i586' 'mips')
url="http://www.musl-libc.org"
license=('MIT')

# Dependency information
groups=('base')
# Depends: toolchain/gcc-static toolchain/binutils packages/linux-api-headers
makedepends=('linux-api-headers')
provides=('libc')
conflicts=('libc')

# Building information
source=("http://www.musl-libc.org/releases/${pkgname}-${pkgver}.tar.gz")
md5sums=('42875e0c111aa1cb9d08663f8d42c799')

build() {
    cd "${srcdir}/${pkgname}-${pkgver}"

    CFLAGS="${_target_cflags}" CXXFLAGS="${CFLAGS}" \
    ./configure --target="${_target_triplet}" \
                --prefix="${_prefix}" \
                --syslibdir="${_libdir}" \
                --libdir="${_libdir}" \
                --bindir="${_bindir}" \
                --includedir="${_includedir}" \
                --disable-shared

    make
}


package() {
    cd "${pkgname}-${pkgver}"

    # Install musl
    make DESTDIR="${pkgdir}" install
}

# Maintainer: Alastair Hughes < hobbitalastair at yandex dot com>

# Package information
pkgname='toolchain'
pkgver='1.0'
pkgrel=4
pkgdesc="Meta-package for the toolchain"

# Additional information...
arch=('any')

# Dependency information
groups=("${_target_triplet}")
depends=("${_target_triplet}-"{binutils,gcc,pkg-config})

package() {
    :
}
